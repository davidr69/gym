---
server:
  session:
    timeout: 300
  port: 8444

database:
  driver: org.postgresql.Driver

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,mappings
    health:
      enabled: true
    prometheus:
      enabled: true
    mappings:
      enabled: true
