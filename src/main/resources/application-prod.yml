database:
  url: jdbc:postgresql://dev-db.lavacro.com:5432/workout

spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update
  data:
    mongodb:
      host: mongodb.lavacro.net
      port: 27017
      database: workout
